#include "Kruskal.h"
#include <iostream>

void Kruskal::runMatrix(MatrixGraph& graph) {
    (void)graph;

    std::cout << "Algorytm Kruskala dla macierzy bedzie dodany pozniej.\n";
}

void Kruskal::runList(ListGraph& graph) {
    (void)graph;

    std::cout << "Algorytm Kruskala dla listy bedzie dodany pozniej.\n";
}