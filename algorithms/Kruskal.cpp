#include "Kruskal.h"

void Kruskal::runMatrix(const MatrixGraph& graph, bool printResult) {
    run(graph, printResult);
}

void Kruskal::runList(const ListGraph& graph, bool printResult) {
    run(graph, printResult);
}