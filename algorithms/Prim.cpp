#include "Prim.h"

void Prim::runMatrix(const MatrixGraph& graph, bool printResult) {
    run(graph, printResult);
}

void Prim::runList(const ListGraph& graph, bool printResult) {
    run(graph, printResult);
}